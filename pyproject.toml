[project]
name = "fairshare"
version = "0.1.0"
description = "Shared household chore tracker with claim-based assignment and point-based fairness tracking"
requires-python = ">=3.14"
dependencies = [
    "django>=6.1.1",
]
